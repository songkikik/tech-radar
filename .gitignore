# 데이터는 절대 커밋하지 않는다 (레이크하우스는 R2/로컬 data/ 에만 존재)
data/
*.duckdb
*.duckdb.wal
*.ducklake
*.ducklake.files/
*.parquet

# 시크릿
.env
.envrc.local

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
# dbt 를 레포 루트에서 실행하면 여기에도 생긴다
logs/
# dbt 익명 사용추적 UUID. 머신마다 다르고 공유할 이유가 없다.
.user.yml

# 모델 캐시
.cache/
